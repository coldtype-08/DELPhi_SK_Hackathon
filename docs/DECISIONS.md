# 결정 기록

> 형식: `- MM/DD [결정자] 결정 내용 — 이유 한 줄` (30분 룰 발동 시 필수 기록)

- 08/11 [초기 설정] Console=다크 고정, Field=라이트 고정 — 현장(주간)·상황실 대비 스토리 + 데모 시 두 앱 구분
- 08/11 [초기 설정] main 브랜치 단일 운용 + 폴더 오너십 — 3인 비개발자 팀에 브랜치 전략은 과함
- 08/11 [초기 설정] DB는 SQLite — 데모 규모에 충분, 배포·설정 리스크 제거
- 08/11 [초기 설정] v0.1 스키마에서 POST_STROKE 의도적 제외 — SCP→v0.2 승격 시연 각본
- 08/12 [건태] 브랜드 팔레트를 로고 기준(네이비 #00206C + 오렌지 #F08A1E)으로 전면 개정, violet/cyan 폐기 — 로고 정렬 + "흔한 다크+네온" 룩 탈피
- 08/12 [건태] demo/index.html을 랜딩페이지에서 키노트형 10장 슬라이드 덱(←→ 키·스냅 스크롤)으로 전환 — 본선 발표자료 겸용
- 08/12 [건태] 공식 브랜드 HEX 확정: 네이비 #162661 + 오렌지 #EF8B1C (+ sky/amber/cream 확장 허용) — 추출값(#00206C/#F08A1E) 폐기
- 08/12 [건태] 덱 룩을 "Paper & Glass"로 확정: 밝은 배경 통일 + 글래스 카드, 좌측 색선 박스 금지, 타이포 확대, 커버 스피너형 모션 제거
- 08/12 [건태] 루프 다이어그램은 오렌지 아크 회전에 맞춰 계층 카드가 순차 등장하는 시퀀스로 연출
- 08/12 [건태] Console 앱도 라이트 확정 (다크 대시보드 금지) — 레퍼런스: Lendora풍 크림 라이트 + 빅넘버 + 필 배지
- 08/12 [건태] 아이콘 자체 제작 금지 → Lucide(오픈소스, shadcn 기본)로 통일. 차트는 에셋 없이 SVG 직접 제작
- 08/12 [건태] 덱 S7 데이터 흐름 차트 최종 제거 — 두 번 의심받은 비주얼은 설명 비용 > 정보 가치. 메시지는 S4 루프·S5 원칙·라이브 데모가 담당
- 08/12 [건태·정리] Contract 버전 변경은 "추가만 허용" + 버전 경계 주석 + 재추출은 별도 레코드 (docs/02 §7.5 신설)
- 08/12 [건태·정리] 검토는 "위험 기반 검토"로 용어 통일, 검토 큐는 결정론적 정렬 (docs/02 §5.5 신설) — "필요한 부분만" 같은 주관적 표현 금지
- 08/12 [건태] 대표 가설 교체: 노인 DDI(→ In-label HYP-002로 강등) → **청소년(12–17세) 연령 확대(HYP-001, Development)**. 이유: 노인은 이미 허가 범위 내 '치료 장벽'이라 확장 가설로 성립하지 않음. 청소년은 성인 허가(18+) 밖이라 Development 판정이 명확하고 상업 액션 차단 시연이 가능
- 08/12 [건태] `label_scope`(IN_LABEL/OUT_OF_LABEL) 필드 신설 — 허가 범위 판정을 문서 원칙이 아니라 데이터 제약으로 강제 (절대 규칙 #5 코드화)
- 08/13 [건태] docs 전체를 `예선기획서_DELPHI_0812_최종.docx` 기준으로 동기화. 이후 기획서 = 대외 약속, docs = 구현 스펙, 방향은 기획서→docs 단방향 (docs/06 §4.5)
- 08/13 [건태] `purpose_domain`(MEDICAL/COMMERCIAL/SAFETY/PUBLIC_EVIDENCE) + 롤별 조회 매트릭스 신설 (docs/02 §9) — 기획서가 "일반 요약 도구와 구분되는 기술적 근거"로 명시한 목적 분리를 프론트 필터가 아닌 쿼리 제약으로 구현. COMMERCIAL 롤은 원문·hcp_ref 접근 불가 + distinct_hcp≥3 집계만
- 08/13 [건태] 기획서의 `Candidate → Reviewed → Approved`를 **2전이로 구현**(REVIEWED 상태 미도입, reviewed_by/at로 기록) — 상태 하나를 줄여 UI·쿼리 단순화. 발표에서는 대응관계로 설명 (docs/01 §3)
- 08/13 [건태] MVP LLM은 **Claude 단일** 채택 (기획서는 "OpenAI 및 Claude" 표기) — 2주 안에 두 벤더 structured output 차이를 관리할 여유 없음. 교체 지점은 `llm.py` 한 곳이라는 사실로 기획서 취지 유지 (docs/01 §8)
- 08/13 [건태] Safety·Risk Agent를 P1→**P0(결정론 최소 구현)**으로 이동 — 기획서 시나리오 ④가 4개 에이전트를 명시하므로 캐시 라벨 대조만이라도 필수. LLM 해석 계층은 P1 유지
- 08/13 [건태] NOT_BOARD_READY 판정을 4개 사유 코드로 결정론화 (docs/01 §3) — "근거 부족"을 사람이 판단하면 재현되지 않음
- 08/13 [건태] 외부 근거 해석 한계 문구를 소스별로 고정 문장화 (docs/02 §10) — LLM이 caveat을 생성하면 문구가 흔들려 규제 리스크. 커넥터가 자동 부착
- 08/13 [건태] 정량 KPI(Data Latency 70%↓ 등)는 **파일럿 검증 대상**으로 명시, 해커톤 완료 기준은 "대표 가설 1건 완주 + 반복 테스트 통과" (docs/00 §1) — 달성했다고 표현하지 않는다
- 08/13 [건태] 발표 덱·목업의 정본은 **레포 루트 `Demo_Mockup.html`**, `demo/` 사본은 8/12 스냅샷 (docs/05 머리말)
- 08/14 [건태] 심사위원 접속 "상시" 기준 확정 → 클라우드 배포를 P2에서 P0로 승격 — Railway 단일 플랫폼(3서비스+볼륨, 싱가포르 리전), 8/24 첫 배포, 9/3 심사 URL 공유. 라이브 발표 정본은 여전히 로컬+캐시 (docs/07 신설)
- 08/14 [건태] 배포 후에도 main 단일 브랜치 유지 — 안정성은 마일스톤 태그(w1-exit, demo-final 등) + Railway 배포 롤백으로 확보. 9/3 심사 URL 공유 후에는 리허설 통과본만 push
- 08/14 [건태] 맥스튜디오는 개발서버·심사 호스팅으로 쓰지 않음(발표장 백업 머신만) — SQLite 파일 DB 구조라 공유 dev 서버가 불필요하고, 가정용 네트워크는 심사 기간의 단일 장애점
- 08/14 [건태] 코퍼스 포맷: 정본은 .txt(문자 오프셋의 기준) + 같은 원문의 docx/pdf "포장" 렌더 — 실물 양식은 가림 캡쳐에서 구조만 명세로 추출(사내 문서 캡쳐 반입은 이것으로 종료). 업로드 인제스트 연출은 PARKING_LOT
- 08/14 [건태] 실물 원석이 영어·"여러 HCP 묶음" 하이라이트형임을 확인 → `HIGHLIGHT_DOC` 유형 신설 + 추출이 HCP 블록 단위로 interaction 분리(docs/02 §1 필드 3종 추가) — 원석=EN, Field 수집·음성=KO, 데모에서 두 언어 모두 시연. 다국어 원칙: 스키마 언어중립 · verbatim 원언어 보존 · 정규화 계층이 언어 흡수 (docs/02 §3)
- 08/14 [건태] Contract 부트스트랩 채택: 시드 스키마 v0.1도 "코퍼스 샘플 → AI 초안 → 사람 대조·확정"으로 만든다(90분 상한) — "0→1도 AI 제안, 1→N도 AI 제안(SCP), 확정은 항상 사람" 서사를 사실로
- 08/14 [건태] Sense 시각화 채택: 추출 리플레이 연출(저장 결과의 결정론적 재생, 라벨 "추출 완료·결과 재생") + 파이프라인 상태 보드(SQL 카운트) — 둘 다 P1 폴리시
- 08/14 [건태] 코퍼스 본문(prose)은 **Claude Code 세션에서 직접 작성 → `scripts/corpus_bodies/*.json` 커밋**을 기본 경로로. API 키 없이 진행 가능하고 랜덤성이 없어 재현성이 API 경로보다 높다. API 경로는 대안으로 유지(교체 지점 1곳) — 단 백엔드 실행에는 `ANTHROPIC_API_KEY`가 여전히 필수(추출·Screen·Board)
- 08/14 [건태] 영어 원석에서 HCP를 성별 대명사(he/she)로 지칭하지 않는다 — 가명 HCP의 성별을 이름으로 추정하지 않고, 각본 문장과 HCP 명부 간 불일치 버그(실제로 H02·M24에서 발생)를 검증기로 차단. 역할 명사·they·무주어를 쓴다
- 08/14 [건태] PDF 원석 = 합성 학회 참관 보고서 3건 (`CONGRESS_REPORT` 신설, 다중 HCP 블록 분리 대상) — 논문·학회 포스터 "원본"은 반입하지 않음(저작권 + 연구 결과물은 Screen의 외부 공개 근거 영역이라 Sense에 섞으면 사내데이터/공개근거 분리가 흐려짐). 포스터 리얼리티는 보고서 속 "포스터 세션 대화" 장면으로 연출
- 08/15 [건태] **스캐폴딩 선행** — 건태가 8/15–20에 backend 뼈대(스키마·시드·stub API) + 두 앱 셸 + 수직 슬라이스 1줄기를 구축하고, 8/21 킥오프는 "각자 실제 기능 착수"로 시작. 역할: 건태=아키텍트·통합자·Console 오너, **stub은 건태·로직은 폴더 오너** — "각자 빈 화면" 제거 + 통합 지옥 예방 (docs/06 §2.5)
- 08/15 [건태] 협업 모델 공유는 GitHub docs가 정본 — 별도 문서 배포 없이 docs/00·06·CLAUDE.md 갱신 + 메신저에는 링크 한 줄 (단일 진실 원칙 유지)
- 08/15 [건태] Next.js는 스캐폴딩 시점 최신 안정판 **16.3** 채택 (docs 표기 15→16 갱신) — 버전 정합 문서가 node_modules/next/dist/docs에 번들되어 팀원 Claude 세션이 정확한 API를 참조. 각 앱의 AGENTS.md 관리 블록은 유지, 앱별 규칙은 apps/*/CLAUDE.md에 추가
- 08/15 [건태] 첫 배포를 8/24 → **8/15~16으로 앞당김** — 워킹 스켈레톤의 정의가 "배포까지 관통"이고, 빈 껍데기 상태에서 배포 배선을 붙이는 게 기능이 쌓인 뒤보다 훨씬 싸다. 시드 로직을 backend 패키지로 이동(app/seed.py)해 서버가 빈 볼륨을 스스로 시드하게 함 + 리셋 API(RESET_TOKEN 보호) 추가 (docs/07 §1.5)
- 08/17 [건태] 프론트에서 Next 자체 gzip 압축 비활성화(`compress: false`) — Railway 엣지와 겹쳐 chunked 종료 청크가 유실되고 Chrome이 응답 전체를 거부(백지). 압축은 엣지가 담당. 브라우저 1종만 보고 검증 완료로 판단하지 않는다
- 08/18 [건태] 역할을 **층 기준으로 개편** — 건태=도메인(Contract 스키마·프롬프트·추출 평가·가설/Contract 화면·발표), 인혁=엔진(backend 파이프라인 전부, 8/21–25 건태 페어), 소정=화면(Field 전체 + Console 홈/Review/Safety + 디자인 공통, Console은 Week 2 합류). 근거: 대체 불가능성 — 실데이터 300건을 읽은 사람이 "뭘 뽑을지"를 쥔다. Console은 화면 단위 분할로 파일 충돌 0 유지
- 08/18 [건태] **9/1부터 배포 게이트** — 팀원은 dev 브랜치에 push, 건태 확인 후 main 병합 시 배포 (Railway 설정 불변). 그 전에는 main 직push(속도 우선, 롤백 1분). "배포 전 사전 확인" 요구를 브랜치 하나로 구현
- 08/20 [건태] **ADMIN(시연·운영) 롤 추가 — 열람 전용** — 데모는 관리자 로그인으로 시작해 전체를 보여주고, 롤 스위처로 "각 롤에겐 이것만 보인다"를 대조 시연. 쓰기·승인 권한은 없음(권한 분리 원칙 유지), 파일럿에선 SSO+접근 로그 전제 (docs/02 §9 · 04 §0)
- 08/20 [건태] **경쟁·시장 축 신설 + Open Payments는 "제조사 집계"로만 쓴다** — 전문가 관여 탭을 개인 단위로 깊게 하자는 제안(처방량 API·Open Payments 개인 조회)을 **기각**. 근거 네 가지: ① 절대 규칙 #7이 "개별 HCP 처방성향 점수화"를 이름으로 금지 + docs/00 리스크 표의 "상용 처방 데이터 반입 금지", ② 우리 `hcp_ref`는 가명이고 실명 매핑은 시스템 밖 — 외부 DB는 NPI 키라 조인하려면 가명을 풀어야 함, ③ 지불 기록×처방 행태의 개인 단위 결합은 실무 반부패 조사의 1순위 패턴, ④ Open Payments는 미국 제도이고 국내 대응물(약사법 §47-2 지출보고서)은 공개 조회 DB가 아님. **대신 집계 축을 개인→제조사로 뒤집어 채택**: `제조사 × 전문과 × 연도` 연구비 집계는 개인 0건이면서 경쟁사 파이프라인 의도의 선행지표가 된다. `/market` 화면 신설(허가 연령 지도·경쟁사 청소년 시험 타임라인·문헌 추이·모수) — 전부 공개 출처, **예측·추세선 금지**(#7의 매출 예측 조항), **FAERS로 경쟁사 안전성 비교 금지**(자발보고는 분모가 없어 발생률 비교가 방법론적으로 불가), **경쟁 약물 오프라벨을 기회로 프레이밍 금지**(#5). COMMERCIAL 롤에 `PUBLIC_EVIDENCE` 도메인을 열되 DEVELOPMENT 연결 근거는 억제 — 권한 분리가 "상업 무조건 차단"이 아니라 "목적에 맞는 범위"임을 보이는 축 (docs/01 §5 · 02 §9)
- 08/20 [건태] **경쟁 약물의 값은 추정해 채우지 않는다** — 초안의 연령 지도에서 우리 라벨(18세+·초점발작)만 확정값으로 그리고 경쟁 5종은 "openFDA 스냅샷 대기" 트랙으로 둔다. 이유: 라벨은 수시로 개정되고, 틀린 시장 사실은 틀린 가설보다 되돌리기 어렵다(스크린샷이 사내에 돌아다닌다). 값은 8/26 fixture 작업에서 실제 스냅샷으로 채운다 (docs/07 §3)
- 08/20 [건태] **루프를 "신호-우선"으로 재배열** — 후보 레이더(CANDIDATE 포함 잠정 집계, 용도는 검토 큐 정렬·가설 트리거 두 가지로 제한) → 임계값(반복 ≥5 ∧ 독립 HCP ≥3, UNMET_NEED·TREATMENT_BARRIER 한정) 도달 시 가설 DRAFT **자동 생성**(승인 0건이면 NO_APPROVED_BASIS로 노출) → 그 가설이 참조하는 claim만 표적 검토. 이유: "전수 검토 후 가설" 인상 제거 — 검토는 활용 조건(§5.5)이라는 원칙을 화면 순서로 구현. 공식 집계·KPI는 여전히 APPROVED만(절대 규칙 #3 불변). 임계 기본값은 코퍼스 각본과 검산: HYP-001·002 둘만 생성 (docs/01 §3 · 02 §5.6 · 04 §3)
- 08/21 [건태] **"레이턴시를 줄인다"를 정의·메커니즘·증명 화면으로 분해** (docs/00 §1.2 신설). 기존 문서는 목표 숫자(Data 70%↓·Insight 50%↓)만 있고 **어느 단계가 어느 지연을 줄이는지가 없었다** — 발표의 마지막 문장이 어떤 화면으로도 뒷받침되지 않는 상태. 4종으로 고정: Data Latency(원석→집계 가능 구조화, 줄이는 것=자동 추출+Field 현장 구조화) · Insight Latency(신호 존재→판단 가능 형태, =임계값 자동 가설 생성) · 반복 재분류(=Contract+vocab+SCP) · **실행 지연**(판단→현장 반영, =실행 루프 Action Item, 08/21 신규). 각 행에 화면 증명 경로를 붙였고, "우리가 70% 줄였다"는 말은 금지로 명시(숫자는 파일럿 baseline 비교). Insight Latency는 §1.5 체크리스트 7항목에 대응 항목이 없음을 명시 — 가설 자동 생성이 기획서 이후 추가된 설계라서다
- 08/21 [건태] **정정 — 110명은 "만난 의사 수"가 아니라 KOL 명부 수였다. 두 모집단을 분리해 적는다.** 앞선 결정에서 두 지표를 하나로 섞어 "재방문 때문에 110명"이라고 해석했는데 틀렸다. 실제로는 ① **만나는 의료진 = 주 7~25명 → 연간 수백 명 규모**, ② **KOL 명부 = 약 110명(2025년 9월까지 집계분)**으로 ②는 ①의 부분집합이다. KOL 명부는 선별 목록이라 100명대가 정상이며 "적다"고 볼 대상이 아니다. **표기 결정**: 개별 의료진 수는 "연간 수백 명"처럼 규모로만(주차 편차가 커서 단일 값이 오히려 부정확하고, 내부 활동량을 노출할 필요도 없다), KOL 수는 집계 시점 병기. **그리고 이 간격이 AS-IS의 위치다** — 연간 수백 명을 만나는데 관리되는 것은 110명이고, 시장 신호는 명부 밖 일반 처방의에게서 더 많이·먼저 나온다. 우리 임계값의 "독립 HCP ≥3"이 **KOL 여부를 묻지 않는다**는 점이 여기에 직접 대응한다 — 사람에게 등급을 매기지 않고 발언을 세기 때문(절대 규칙 #7)에 명부 밖 신호를 놓치지 않는다. 미집계 구간도 갱신: 2025년 10월~2026년분이 최근 수령됐으나 아직 반영되지 않은 상태 (docs/08 §1.2·§1.2.1 · 03 §1)
- 08/21 [건태] **"의사 110명"을 얼버무리지 않고 집계 범위를 명시한다 — 미집계 구간 자체가 AS-IS의 증거다.** 110명은 전체 누적이 아니라 **2021~2024년 자료만 집계한 결과**이고, 2020년 일부와 2025~2026년분은 집계되어 있지 않다(전체 기간의 약 1/3). "수백 명"으로 뭉개는 안을 기각한 이유: ① 심사 기준이 "문제 정의의 명확성"이라 검증 가능한 진술이 뭉갠 진술보다 강하다 ② **미집계 구간의 존재가 우리 주장의 실물 증거**다 — 왜 안 세어졌나에 대한 답이 "세는 일이 사람 손으로 하는 일이기 때문"이고, 이 문서를 쓰려고 규모를 확인하려 했을 때 팀도 최신 구간 숫자를 몰랐다. 즉 §1.1의 "쌓이는 것이 보이지 않는다"가 추상적 주장이 아니라 **AS-IS를 설명하려다 직접 부딪힌 기록**이 된다. 표기 원칙: "2021~2024년 자료 기준 110명"처럼 범위를 함께 적는다 (docs/08 §1.2·§1.2.1)
- 08/21 [건태] **XCOPRI 기간 기준 확정 — FDA 승인 2019-11-21, 미국 출시 2020년 2분기 → 현재까지 약 6년 3개월.** 표기는 "미국에만 출시"가 아니라 **"SK가 직접 판매하는 미국 시장 기준"**으로 쓴다 — 유럽은 파트너사가 다른 브랜드로 공급하므로 전자는 사실 확인에서 걸릴 수 있다. 출처는 SK Life Science FDA 승인 보도자료 (docs/08 §1.2)
- 08/21 [건태] **AS-IS 재정의 — 병목은 "분석이 느리다"가 아니라 "쌓이는 것이 보이지 않는다".** raw data 확인 결과(주 7~25명 면담 · 의사당 코멘트 2~3개 → 주 14~75건 · 연 700~3,900건, 실 등장 의사 약 110명) 팀의 실제 문제 서술이 "몇 주 걸린다"가 아니었다: **가설은 어느 정도 쌓인 뒤에 나오는데, 얼마나 쌓이는지 자체를 알 수 없었다.** 세는 장치가 없으면 임계값을 알 수 없고, 그러면 가설이 사람의 기억에 의존한다. → TO-BE의 첫 기능은 요약이 아니라 **계수기**이고, 후보 레이더(docs/02 §5.6)가 UI 위젯이 아니라 AS-IS의 핵심 결함을 겨냥한 기능이라는 근거가 여기서 생긴다. **의사 110명 관찰이 우리 설계의 근거**로 전환됨: 110명이 수천 건을 만든다 = 반복이 기본값 → "한 사람이 5번"과 "5명이 각각"을 구분하는 것이 분석의 거의 전부 → 임계값이 반복 수와 독립 HCP 수를 따로 보는 이유 (docs/08 §1.1·§1.2)
- 08/21 [건태] **외부 사례 확보 — 산업 공통 과제임을 근거로 제시.** 업계 설문(Within3/Reuters): 의료부문 팀 **91%가 수집 데이터의 대부분을 인사이트로 쓰지 못하고**, **65%는 인사이트 부재가 제품 출시를 위험에 빠뜨린다**고 답. 업계 문헌의 문제 서술이 우리와 동일(자유 텍스트 = taxonomy 없음·수 주간 방치·담당자 간 패턴 탐지 불가), **권장 해법도 동일**(구성된 입력 폼·insight taxonomy·필수 항목 = 우리 Field 동적 폼 + Data Contract). 벤더(Veeva Link)는 "months → hours"를 목표로 제시 — 우리 팀의 "제대로 분석하면 몇 개월" 추정과 같은 시간 척도. 업계 지표명 `insight-to-action rate`·`insight half-life`가 우리 Insight Latency·의사결정 속도와 대응. **인용 규칙 명시**: 벤더 주장은 출처 유형을 병기하고 목표치 근거로 삼지 않으며, 설문 수치는 원문 확인 후 인용 (docs/08 §1.3)
- 08/21 [건태] **기대효과는 기획서 §6-2 원문 4개를 그대로 유지하고 5번째를 더한다.** 기획서 확정 표현: Data Latency 70%↑ 단축 / Insight Latency 50%↑ 단축 / 반복 데이터 정리·재분류 70%↑ 감소 / 핵심 정보 reviewer acceptance 90%↑. 여기에 **의사결정 속도 향상**(판단 → 현장 반영, 실행 루프)을 5번째로 추가. **reviewer acceptance를 빼지 않는 이유 두 가지**: ① 기획서는 대외 약속이고 심사위원이 예선본과 나란히 볼 수 있다 ② 해커톤에서 **유일하게 실제 수치를 낼 수 있는 항목**(ground truth 대조)이라 3_검증결과의 핵심 근거다. 5번 추가 근거는 기획서 §6-2의 궁극 목표 사슬 안에 이미 있다("Growth Hypothesis 검토 가속 → 실행·근거 생성의 우선순위 고도화") — 예선 시점에 이름이 없었고 08/21 실행 루프 1급화로 측정 가능해졌다 (docs/08 §4)
- 08/21 [건태] **코퍼스는 재생성하지 않는다 — 실데이터보다 반복률이 낮은 것이 유리한 방향.** 실데이터는 의사 110명이 1인당 수십 건, 코퍼스 v3는 300인이 1인당 3.7건. 현실이 더 반복적이므로 **우리 임계값(반복 ≥5 ∧ 독립 HCP ≥3)은 보수적**이고, 실데이터에서는 신호가 더 빨리 임계를 넘는다. 데모 수치·목업·검산이 이미 v3에 정합돼 있어 재생성 비용이 크므로 그대로 두고, 파일럿 임계값 재조정 필요성만 기록 (docs/03 §1)
- 08/21 [건태] **주최측 본선 기준 반영 — 전제 2개가 틀려 있었다.** ① 마감은 09-04가 아니라 **09-03(목) 23:59**이고 이후 변경은 반영되지 않는다 → 일정 재역산(9/2 제출물 조립, 9/3 마감, 9/4~9/9 심사 기간 코드 동결). ② **본선은 발표가 아니다** — 심사위원이 폴더를 읽고 배포 앱을 직접 사용하며 "문서에 없는 내용은 반영되지 않는다". 따라서 "라이브 발표 정본은 로컬"이라는 기존 원칙은 **결선(09-14~15)에만** 해당하고, 본선 정본은 **제출 문서 + 배포 URL**이다. 파생 결정: Week 2 Exit에 "팀원 아닌 사람이 안내문만 보고 완주" 항목 추가(구현 완성도 25점의 실제 측정 방식), 심사 기간 상시 가동 요건을 docs/07 §4.5로 신설(자정 자동 리셋·유료 전환·코드 동결). 결선 3인 전원 참석 가능 확인 (docs/00 헤더·§1.5·§3 · 07 §4.5)
- 08/21 [건태] **`docs/08_PROBLEM_AND_IMPACT.md` 신설 — 배점 50점(SUPEX 30 + 혁신성 20)이 보는 문서.** AS-IS를 **축적분(stock) / 유입분(flow) 두 갈래**로 나눈 것이 핵심 프레이밍: 문서 요약 도구는 stock만 다루고, stock만 고치면 다음 달에 같은 문제가 다시 쌓인다 → DELPHi가 입구를 둘(Sense·Field) 갖는 이유가 여기서 설명된다. 또 **"AI Readable data"를 수식어가 아닌 5조건 사양으로 정의**(스키마·근거 포인터·목적 태그·언어중립 canonical·상태) — 벡터DB에 텍스트를 넣는 것으로는 4개를 못 얻는다는 대비. AI 네이티브는 "얼마나 많이 쓰나"가 아니라 **AI/사람/결정론 코드 3분할**로 제시. 확장성은 주장이 아니라 코드 위치로 지목(llm.py 한 파일·contract yaml·vocab 계층·ANSI SQL 제약). 기대효과 4종 + 실행 지연에 **측정 설계**를 붙였고, AS-IS baseline 실측 2개는 ⟨확인 필요⟩로 남겼다 — 없으면 목표치로만 제시(허위 정밀도 금지)
- 08/21 [건태 — Data Steward] **Contract v0.1 재확정 승인.** `patient_segment`에 `GENERALIZED_PGTC`·`LGS` 추가(둘 다 자동 `OUT_OF_LABEL`)를 Steward 권한으로 승인한다. 첫 추출 실행 전 시드 개정이므로 부트스트랩의 연장이며, **이 시점 이후의 모든 스키마 변경은 SCP 경로만 유효**하다(절대 규칙 #4). 이 승인이 없으면 PGTC·LGS 신호가 임계 집계에 서지 못해 HYP-003·004가 생성되지 않는다 (contract_v0_1.yaml · docs/02 §2)
- 08/21 [건태] **COMMERCIAL × PUBLIC_EVIDENCE 개방을 승인하되, 도메인을 넓히지 않고 데이터를 나눠서 강제한다.** `PUBLIC_EVIDENCE`에 성격이 다른 두 종류가 섞여 있던 것이 문제였다: 시장 참조 데이터(누구나 조회 가능한 공개 사실)와 가설 연결 근거(가설의 존재·주제를 드러냄). **`external_refs` 테이블을 신설**해 전자를 분리했다 — `hypothesis_id` 컬럼이 **아예 없으므로** COMMERCIAL 개방이 구조적으로 안전하다. `screen_findings`는 그대로 가설의 게이트를 따른다(`h.kind <> 'DEVELOPMENT'`), 직접 지목 조회는 403·목록은 행 억제+suppressedRowCount. 회귀 테스트 1개(`test_access_commercial.py`)를 안전장치로 못박았다 — DEVELOPMENT 가설의 id·문장이 COMMERCIAL 응답에 단 한 건도 나타나지 않음을 확인. 이 개방이 새면 절대 규칙 #5가 무너지고, 제대로 되면 "권한 분리는 무조건 차단이 아니라 목적에 맞는 범위"의 가장 좋은 증거가 된다 (docs/02 §4·§9.5 · 04 §4.5)
- 08/21 [건태·소정] **경쟁 약물 허가 연령은 예시값으로 채우지 않는다 — 소정이 openFDA 커넥터를 실물로 연결한다.** 초안의 연령 지도는 경쟁 5종을 "스냅샷 대기" 트랙으로 유지하고, 값은 `external_refs(ref_type=LABEL_AGE)`에 실제 응답이 적재될 때만 채운다. 오너십: openFDA·CT.gov 커넥터 = 소정(기존 Field 오너십에 추가). 추정값 금지 이유는 08/21 앞 결정과 동일 — 틀린 시장 사실은 틀린 가설보다 되돌리기 어렵다
- 08/21 [소정] **Field STT API 선택은 다음 주 월요일(8/24) 실기 테스트 후 결정 — 보류.** 후보: Deepgram Nova-3(ko/en 각각 monolingual + Keyterm에 우리 vocab 주입, $200 크레딧) vs Soniox(문장 중간 한↔영 전환, 단가 높음). 결정 전까지 Field 데모는 전사 스크립트 재생 폴백으로 완주 가능하므로 다른 작업을 막지 않는다 (docs/03 §6 · CLAUDE.md 기술 스택 "음성 P1")
- 08/21 [건태·회의] **코퍼스 v3 — 320문서 / 1,118단위 / HCP 300인 / 2021-01~2026-08 생성 완료.** v2 스펙(90건)을 건너뛰고 바로 v3로 갔다: 5년 반의 축적분처럼 보여야 "쓰지 못하던 원석"이라는 전제가 산다. 문서당 의사 수 확대(하이라이트·학회 **8~14 블록**, 다중HCP 82문서 평균 10.7), 학회 보고서 3→**22건**. 신호 실측: S1 52/34인 · S6 41/27인 · S7 34/23인 · S8 21/15인 · S3 19/14인 · S4 47 · S5 38 · S2 27 · X 4 (총 283건, 노이즈 860단위=77%). **임계 통과 조합이 정확히 4개**임을 생성기가 문자 그대로 검증한다 (docs/03 §1·§2 · 01 §3 실측 반영, docs/04 예시 수치 동기)
- 08/21 [건태] **본문 생성을 조합형 산문 엔진으로 전환** (`scripts/corpus_v3.py`) — 1,118블록은 손으로 못 쓰고 이 환경에선 Claude API도 못 쓴다(키·망). **손으로 쓴 문장 조각 + 시드 고정 PRNG** 조합으로 LLM 없이 완전 재현(같은 커밋=같은 코퍼스). 기존 165블록은 그대로 재사용. 신호 문장은 frame×detail에서 **전역 유일**하게 뽑고(같은 문장 두 번 없음), 노이즈는 주제 풀 9종 × 작성자 문체(narrative/terse/email) × 시기별 주제. 트레이드오프: LLM 산문보다 표현 폭이 좁다 → 8/22 리얼리티 검수에서 flag된 조각을 보강하는 방식으로 개선한다
- 08/21 [건태·회의] **학회 보고서에 실제 학회명 사용 — 단, 경계를 코드로 고정.** 팀 결정으로 실명을 쓰되 **연례 개최가 확실한 미국 학회 3곳만**(AES 12월·AAN 4~5월·ANA 9~10월) 쓴다 — 격년 학회(IEC·ECE)는 **연도 짝을 틀리면 그 자체로 허위 기록**이라 배제. 명칭·약어·연도·개최 월까지만 쓰고 **개최 도시·정확한 일자·세션 내용·초록·포스터·발표 데이터는 쓰지 않는다**(내용은 우리 MSL의 가상 대화뿐). 문서 첫머리에 합성 고지 다음 줄로 **학회 참조 고지** 별도 삽입. 학회형을 늘린 이유: 한 문서에 HCP가 가장 많이 담겨 블록 분리가 잘 드러나고, 포스터 앞 대화가 **Sense와 Screen이 만나는 이음새**다 (docs/03 §3.6)
- 08/21 [건태] **가설 생성에서 `patient_segment = UNSPECIFIED` 제외 규칙 명시.** 환자군이 특정되지 않은 신호는 가설의 주체가 될 수 없다. v1(6건)에서는 드러나지 않았지만 v3에서 S3(post-stroke)가 19건/14인이라 이 규칙이 없으면 "미지정 환자군 가설"이 5번째로 생성된다. 스키마 밖 표현은 UNSPECIFIED로 떨어지고 `unmapped_terms` 반복이 SCP 경로를 탄다 — 구조 루프의 정의와도 일치 (docs/01 §3)
- 08/21 [건태·회의] **코퍼스 v2 확장 + 발표 서사 "검증→발굴" 재편.** 가설 2개짜리 코퍼스는 "심어둔 것을 찾는" 인상 — **S7(PGTC 11건/7인)·S8(LGS 7건/5인) 발굴 신호를 신설**해 임계값 자동 생성이 4개(HYP-001~004)가 되게 하고, 문서 63→90건/단위 118→≈165, HCP 24→30인, 기간 11→17개월, AE 용어 다양화(7건)로 확장. 서사: HYP-001(청소년)은 회사가 실제 임상·연령확대 진행 중(공개 CT.gov 기준)인 **검증 축**("정답을 아는 문제"의 재발견), 완주 대표는 **HYP-003(PGTC)**으로 재지정, HYP-004(LGS)는 외부 근거가 얇으면 NOT_BOARD_READY로 남기는 것까지가 각본(근거 없으면 순위 없음의 시연 — 8/26 스냅샷 후 결말 고정, 캐시 조작 금지). 생성 일정: 8/22 착수, 8/23 저녁 교체 — 파이프라인은 계약 기반이라 병행 무방 (docs/00 §1·§3 · 01 §3 · 03 §1·2·6·7)
- 08/21 [건태] **Contract v0.1 재확정 — patient_segment에 GENERALIZED_PGTC·LGS 추가 (둘 다 OUT_OF_LABEL).** 집계·가설 축이 patient_segment이므로 발굴 가설이 서려면 시드에 값이 있어야 한다(indication_mention은 text라 임계 집계 불가). **첫 추출 실행 전(킥오프 당일)이라 시드 개정으로 처리** — 부트스트랩의 연장이며, 실행 이후의 모든 변경은 SCP 경로만 유효(절대 규칙 #4). LGS는 08/19에 indication_mention 예시로 남겼던 것을 승격. label_scope 자동판정 규칙에 두 값 포함 — Development 자동 태깅 유지 (contract_v0_1.yaml · docs/02 §2 동일 커밋)
- 08/21 [건태] **폐쇄 루프 프레이밍 정정 — 실행 루프를 1급 개념으로 승격.** 기존 문서가 루프 닫힘을 Contract 변경(SCP→v0.2)에 기울여 서술했는데, 스키마 변경은 드물게 도는 게 정상이고 매일 도는 것은 **실행 루프**(Board 권고 → 사람 승인 → Action Item → Field 체크리스트 → 표적 수집 → 집계 재유입)다 — 레이턴시 단축 주장(docs/00 §1)의 본체. 변경: ① `decisions.follow_up_json`(사장되는 JSON)을 **`action_items` 테이블로 정규화**(15번째 테이블) + 상태 머신 PROPOSED→ACTIVE→COLLECTED/CLOSED, ② `target` enum은 FIELD_CHECKLIST·SPECIALIST_REVIEW·MEDINFO_RESPONSE 셋뿐 — **상업 실행 값이 없어 절대 규칙 #5가 타입 수준에서 강제됨**, ③ 액션별 "참조 수집 카운트"는 `interactions.checklist_refs` 경유 SQL로 APPROVED만 카운트(#1·#3), ④ Board·CEO 제안은 PROPOSED 초안일 뿐 사람 decision에 채택된 것만 ACTIVE(승인 권한 없음 원칙 유지), ⑤ 데모 ⑤에 "체크리스트 전달" 장면 명시, docs/02 §8의 루프 혼동 서술 정정(체크리스트 반영은 Contract 버전과 무관). (CLAUDE.md 한줄요약 · docs/00 P0 · 01 §3·§5 · 02 §1·§4·§8 · 04 §4·§6)
- 08/19 [건태] **Contract 부트스트랩 완료 — v0.1 최종 확정.** 격리 AI 초안(코퍼스 15건만 열람, 원문 보존: docs/assets/bootstrap-ai-draft.md) × 36컬럼 원안 × v0.1 3자 대조. **채택 9**: claims에 solicitation(발언 경위 — AI 초안의 발견)·sentiment(논조, HCP별 집계 금지)·indication_mention·concomitant_drugs·administration_note + signal_type에 REPURPOSING_SIGNAL(자동 OUT_OF_LABEL) + safety에 event_terms·severity_note(언급 표현 그대로, 등급 판정은 PV 관할)·product_named. **제외(사유 기록)**: 용량 수치 5종(허위 정밀도·규제 민감), THINKING_PROCESS/LLM_RAW류(llm_runs가 대체). **보류→SCP**: 직함(CREDENTIAL), msl_response, ae_screening 3값 증명, 채널·시간 메타, MSL·기관 마스터. AI가 독립적으로 용량 수치화 반대·처방자 플래그 금지·governed vocab(=SCP)에 수렴 — sentiment·severity는 1차 권고가 제외였으나 도메인 오너 판단으로 사용 제약을 걸어 채택
- 08/22 [건태] **제출물은 레포에서 쓰고 Drive로 올린다 — `submission/`을 폴더 0~4의 로컬 미러로 신설.** Drive에 직접 쓰면 버전 이력·동시 편집 충돌 감지·코드와의 정합 확인 세 개를 잃는다. 폴더 이름은 주최측 구조와 **1:1로 고정**(이름이 다르면 그 항목은 미제출 처리). `0_본선_안내문`은 주최측 것이므로 **우리 파일을 올리지 않는다**(정리하려고 손대는 것도 금지) — 레포에는 읽기 전용 메모만 둔다. 파생 규칙: 각 폴더 README는 **Google Docs로도 만든다** — Drive의 `.md` 미리보기는 표를 `|`가 박힌 평문으로 보여주므로, 심사위원이 표를 못 읽으면 안 쓴 것과 같다 (submission/README.md · docs/00 §8)
- 08/22 [건태] **개발 일지를 커밋에서 자동 초안 생성한다 (`scripts/daily_report.py`).** 주최측이 자주 틀리는 것으로 지목한 항목이 "4번 폴더 마감 직전 몰아쓰기 — 기억으로 복원한 일지는 티가 납니다"인데, **그 원인은 게으름이 아니라 하루 끝에 "오늘 뭐 했지"를 떠올리는 게 실제로 어렵다는 것**이다. 커밋은 이미 그 답을 갖고 있으므로 사실(무엇을 고쳤나)은 git·LOG·DECISIONS에서 가져오고 사람은 **git이 모르는 것**(왜 막혔나·무엇을 포기했나·오늘의 한 줄)만 쓴다. 표준 라이브러리만 사용(3인 노트북에서 그대로 실행), 기존 파일은 덮어쓰지 않음(`--force` 필요). **Claude Code 세션 커밋은 author가 `Claude`로 찍혀 사람 구분이 git에 남지 않으므로**, 건드린 경로의 오너십(CLAUDE.md)으로 추정하고 `※`를 붙여 사람이 확인하게 했다 — 추정을 사실처럼 적지 않기 위한 표시다 (submission/4_제작과정/README.md · docs/06 §2)
- 08/22 [건태] **WBS는 마스터플랜의 파생물로 둔다 — 두 곳에 계획을 적지 않는다.** `submission/4_제작과정/WBS.md`는 `docs/00 §2·§3`을 산출물 단위로 재배열한 표이고, **계획이 바뀌면 docs/00을 먼저 고치고 WBS를 맞춘다**(반대 방향 금지). WBS에만 있는 것은 **계획 변경 이력** 한 표다 — 배포를 8/24→8/17로 앞당긴 것, 코퍼스를 63→320건으로 재생성한 것, 누락된 P0(실행 루프)를 발견해 올린 것. 심사가 "계획대로 진행했나"를 볼 때 **변경 자체가 아니라 변경의 근거**가 답이므로, 이 표가 WBS의 본체다 (submission/4_제작과정/WBS.md)
- 08/22 [건태] **`BLOCKERS.md`를 소급 작성 — 항목마다 "어떻게 찾았나"를 필수 칸으로 둔다.** 집요함 15점은 *"결과물만으로는 드러나지 않는 부분이라 별도로 기록해 주셔야 확인할 수 있습니다"*가 기준이므로, 증상·해결만 적으면 남는 게 없다. 실제로 시간을 쓴 자리는 **원인을 좁혀 간 경로**다(예: 플랫폼 3곳에서 재현해 보고 나서야 "갈아타도 소용없음"을 알았다 / "실패가 아니라 시작조차 안 했다"로 관점을 바꾸니 볼 곳이 좁혀졌다). 10건 기록 + 미해결 2건 명시. 우리 자신의 오류 3건(코퍼스 검증 6연속 실패·화면이 규칙 위반·규모 해석 오류)을 **빼지 않고 넣는다** — 고친 기록이 안 틀린 척보다 강하다 (submission/4_제작과정/BLOCKERS.md)
- 08/22 [인혁] **STT 3종 비교 랩 신설** (`apps/stt-lab`, 포트 3002, 로컬 전용·배포 안 함) — 8/24 실기 테스트(DECISIONS 08/21 [소정])의 판정 도구. 소정이 좁힌 후보 2종에 Gladia를 더해 3탭으로 비교: Soniox `stt-rt-v5` / Deepgram `nova-3-medical`·`nova-3` / Gladia `solaria-1`. API 키는 화면 입력 후 브라우저 localStorage 보관(커밋·서버 전송 없음)이라 백엔드 없이 브라우저가 벤더에 직결. `lib/stt/`가 이식 대상이고 `app/page.tsx`는 버릴 코드 — 결정 후 `apps/field/lib/stt/`로 복사
- 08/22 [인혁] STT 선택 컷오프 4개 확정: **실시간 스트리밍 · 실시간 화자분리 · keyword boosting · 언어 케이스 정확도.** 순위 기준은 지연(낮을수록). 화자분리를 필수로 둔 근거: `verbatim_quote`가 HCP 발언이어야 하고 `solicitation`(발언 경위, docs/02 §2) 판정이 화자를 전제로 한다
- 08/22 [인혁] 벤더 확인 결과 — **SDK 소스·정본 스펙 기준(마케팅 문구 아님)**. Deepgram: `diarize` Deprecated→`diarize_model=v1`, `keyterm` 500 tokens·Nova-3 전용·가중치 미지원(붙이면 조용히 무시), 브라우저는 `Sec-WebSocket-Protocol` 인증, **`nova-3`=ko O / `nova-3-medical`=ko X → 의료 특화와 한국어는 동시 불가** / Soniox: `enable_speaker_diarization`·`language_hints`·`context.terms`·임시 API 키 확인, WS 경로·`audio_format`도 08/22 SDK 소스(`client.py:39`)에서 확인 — `wss://stt-rt.soniox.com/transcribe-websocket`·`pcm_s16le` / Gladia: **정본 스펙(요청 OpenAPI·응답 asyncapi `Utterance`)에 실시간 화자분리 없음**, 산문·SDK 응답 타입에는 있어 랩에서 `diarization` 실송해 422/201로 실측. 오픈 가중치·aggregator(HF·fal·Groq·OpenRouter·Whisper+pyannote)는 배치 전용 또는 화자분리 미지원으로 전부 탈락
- 08/22 [인혁] STT 평가 자산 신설 — `scripts/stt_eval/`. **대본 3종, 전부 한국어**(한국인 의사가 화자라는 전제, `docs/03` §1.5): **P1** = HYP-003(PGTC) 신규 작성 7턴 — S7 + AE + 자료요청 + PII를 한 대본에 넣어 시연 장면 5개가 한 번에 나오게 함(초안 — 시연 대본은 소정, 데모 각본·제출물은 건태 판단) / **D1** = `apps/field/lib/capture-demo.ts` 의 7턴 그대로(S1 청소년, 영어 없음) / **T1** = D1에 영어 용어(`XCOPRI`·`lamotrigine`·`DDI`·`titration`)와 전화번호를 얹은 것 — **문장 중간 한↔영 전환을 시험하는 유일한 자산**(코퍼스 한국어 음성 전사에는 영어 단어가 없음, 08/22 전수 확인). 채점은 전체 WER이 아니라 **핵심 토큰 우선**(P1 13개·D1 9개·T1 15개, ★는 틀리면 신호가 소멸하는 항목) — 전사가 `interactions.raw_text`가 되어 evidence 오프셋의 기준이 되므로(docs/02 §1) 되돌릴 수 없다
- 08/22 [인혁] **TTS 합성음은 판정 재료로 쓰지 않는다.** `gen_test_audio.py`(espeak-ng, 무료·오프라인)로 배선 확인용 WAV를 만들 수 있게 했으나, 포먼트 합성이라 발음이 규격화돼 **"17세→70세" 같은 실제 오인식이 재현되지 않고** 한국식 영어 발음도 재현되지 않는다 — 실제로 들어보고 품질이 판정에 못 미친다고 확인했다(08/22). 상용 TTS(edge-tts·piper·ElevenLabs)는 사내 프록시에서 전부 차단(403)되어 채택 불가. **판정 음성은 사람이 직접 만든다** — 2인이 대본을 번갈아 읽어 녹음하거나 별도 TTS 서비스로 생성. WAV는 생성물이라 커밋하지 않는다(`.gitignore`)
- 08/22 [인혁] **비교는 「3종 동시 실행」으로 한다** — 마이크 스트림 하나를 세 세션에 동시에 흘려 **한 번의 발화·재생을 셋이 같이 듣게** 만들었다. 실제 시연은 사람이 말하거나 음성을 스피커로 트는 방식이므로 파일 업로드 경로는 마이크·공기·잡음을 건너뛴 **다른 조건**을 재게 된다(그래서 배선 확인용으로 격하). 그렇다고 서비스마다 따로 재생하면 회차마다 소리가 달라져 서비스 차이와 재생 차이가 섞인다. 부수 발견: 스피커로 틀어 마이크로 받을 때는 브라우저의 **에코 제거·잡음 억제·자동 게인을 전부 꺼야 한다** — 켜두면 스피커 소리를 에코로 판정해 지워버려 서비스 성능이 나쁜 것처럼 보인다(화면에서 「마이크 입력 방식」으로 전환)
- 08/22 [인혁] **STT 3종 비교 문서를 팀 공용으로 남긴다** (`scripts/stt_eval/SERVICE_COMPARISON.md`) — 항목마다 **[확인]/[추정]/[실측]**을 표기했다. 사내 프록시에서 세 벤더의 공개 문서가 모두 막혀(403) SDK 소스와 정본 스펙(OpenAPI·AsyncAPI)을 GitHub에서 직접 읽었고, **마케팅 문구는 근거로 쓰지 않았다** — Gladia에서 실제로 어긋났기 때문이다(소개 페이지엔 실시간 화자분리가 있으나 요청 OpenAPI·응답 asyncapi 어디에도 없음). 탈락 후보의 사유도 같은 문서에 남겨 되짚지 않게 했다(Whisper 계열·aggregator = 배치 전용 또는 실시간 화자분리 미지원 / Web Speech API = 화자분리·부스팅 없음)
- 08/22 [인혁] **판정용 음성을 랩 안에서 만든다 — Gemini TTS 섹션 신설** (`apps/stt-lab` 「대본 → 음성」 탭 + `app/api/tts/route.ts`). 사람 녹음이 정본이지만 팀원 여러 명이 **같은 소리로** 돌려보려면 재현 가능한 고정 파일이 필요하고, espeak-ng는 품질이 판정에 못 미쳤다. **화자 2명을 한 번의 호출로** 만든다(`multiSpeakerVoiceConfig` — google-genai SDK 타입에서 확인) — 한 목소리면 화자분리 컷오프를 검증할 수 없다. **모델 이름은 벤더 목록에서 받아오고 목소리 이름은 자유 입력**으로 뒀다(문서 접근 불가라 추측하지 않는다 — 틀리면 벤더 오류가 화면에 그대로 뜬다). 응답이 헤더 없는 raw PCM(`audio/L16;codec=pcm;rate=…`)이라 라우트에서 WAV 헤더를 붙이는데, **샘플레이트를 응답 mimeType에서 읽고 못 읽으면 24000 가정임을 화면에 표시**한다(하드코딩해 조용히 틀리지 않기 위해). 라우트를 둔 다른 이유는 CORS 불확실성
- 08/22 [인혁] **랩 기본 대본을 D1로 되돌린다.** STT 선택에 실제로 쓰는 것은 D1(main의 `capture-demo.ts` 그대로)·T1(D1 + 영어 용어·PII)이고, **P1은 소정에게 확인 요청할 초안**이므로 선택지로만 남긴다. T1이 별도로 필요한 근거: 코퍼스 한국어 음성 발화 100줄에 **영어 단어가 0건**이어서(08/22 재확인) 컷오프 중 하나인 "언어 케이스 정확도"를 main 자산만으로는 시험할 수 없다
- 08/22 [인혁] **첫 실측(내 키·내 PC) 결과 두 건.** ① Soniox "Start request is malformed" — `context.general`을 dict로 보낸 내 구현 오류. 규격은 `[{key,value}]` 목록(SDK `_coerce_general`이 dict를 이 형태로 변환해 전송하는 것으로 확인) → 수정. ② **Gladia에 `diarization`을 보내면 400 "Invalid parameter(s)" — 실시간 화자분리 미지원이 실측으로 확정**(SDK `LiveV2InitRequest`에도 해당 필드 없음). 컷오프 2번(실시간 화자분리) 탈락. 랩은 거부 증거를 원시 응답에 남긴 뒤 diarization 없이 재시도해 전사 비교는 계속 진행하도록 수정

