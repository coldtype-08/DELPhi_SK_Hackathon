# 결정 기록

> 형식: `- MM/DD [결정자] 결정 내용 — 이유 한 줄` (30분 룰 발동 시 필수 기록)

- 08/11 [초기 설정] Console=다크 고정, Field=라이트 고정 — 현장(주간)·상황실 대비 스토리 + 데모 시 두 앱 구분
- 08/11 [초기 설정] main 브랜치 단일 운용 + 폴더 오너십 — 3인 비개발자 팀에 브랜치 전략은 과함
- 08/11 [초기 설정] DB는 SQLite — 데모 규모에 충분, 배포·설정 리스크 제거
- 08/11 [초기 설정] v0.1 스키마에서 POST_STROKE 의도적 제외 — SCP→v0.2 승격 시연 각본
- 08/12 [건태] 브랜드 팔레트를 로고 기준(네이비 #00206C + 오렌지 #F08A1E)으로 전면 개정, violet/cyan 폐기 — 로고 정렬 + "흔한 다크+네온" 룩 탈피
- 08/12 [건태] demo/index.html을 랜딩페이지에서 키노트형 10장 슬라이드 덱(←→ 키·스냅 스크롤)으로 전환 — 본선 발표자료 겸용
- 08/12 [건태] 공식 브랜드 HEX 확정: 네이비 #162661 + 오렌지 #EF8B1C (+ sky/amber/cream 확장 허용) — 추출값(#00206C/#F08A1E) 폐기
- 08/12 [건태] 덱 룩을 "Paper & Glass"로 확정: 밝은 배경 통일 + 글래스 카드, 좌측 색선 박스 금지, 타이포 확대, 커버 스피너형 모션 제거
- 08/12 [건태] 루프 다이어그램은 오렌지 아크 회전에 맞춰 계층 카드가 순차 등장하는 시퀀스로 연출
- 08/12 [건태] Console 앱도 라이트 확정 (다크 대시보드 금지) — 레퍼런스: Lendora풍 크림 라이트 + 빅넘버 + 필 배지
- 08/12 [건태] 아이콘 자체 제작 금지 → Lucide(오픈소스, shadcn 기본)로 통일. 차트는 에셋 없이 SVG 직접 제작
- 08/12 [건태] 덱 S7 데이터 흐름 차트 최종 제거 — 두 번 의심받은 비주얼은 설명 비용 > 정보 가치. 메시지는 S4 루프·S5 원칙·라이브 데모가 담당
- 08/12 [건태·정리] Contract 버전 변경은 "추가만 허용" + 버전 경계 주석 + 재추출은 별도 레코드 (docs/02 §7.5 신설)
- 08/12 [건태·정리] 검토는 "위험 기반 검토"로 용어 통일, 검토 큐는 결정론적 정렬 (docs/02 §5.5 신설) — "필요한 부분만" 같은 주관적 표현 금지
- 08/12 [건태] 대표 가설 교체: 노인 DDI(→ In-label HYP-002로 강등) → **청소년(12–17세) 연령 확대(HYP-001, Development)**. 이유: 노인은 이미 허가 범위 내 '치료 장벽'이라 확장 가설로 성립하지 않음. 청소년은 성인 허가(18+) 밖이라 Development 판정이 명확하고 상업 액션 차단 시연이 가능
- 08/12 [건태] `label_scope`(IN_LABEL/OUT_OF_LABEL) 필드 신설 — 허가 범위 판정을 문서 원칙이 아니라 데이터 제약으로 강제 (절대 규칙 #5 코드화)
- 08/13 [건태] docs 전체를 `예선기획서_DELPHI_0812_최종.docx` 기준으로 동기화. 이후 기획서 = 대외 약속, docs = 구현 스펙, 방향은 기획서→docs 단방향 (docs/06 §4.5)
- 08/13 [건태] `purpose_domain`(MEDICAL/COMMERCIAL/SAFETY/PUBLIC_EVIDENCE) + 롤별 조회 매트릭스 신설 (docs/02 §9) — 기획서가 "일반 요약 도구와 구분되는 기술적 근거"로 명시한 목적 분리를 프론트 필터가 아닌 쿼리 제약으로 구현. COMMERCIAL 롤은 원문·hcp_ref 접근 불가 + distinct_hcp≥3 집계만
- 08/13 [건태] 기획서의 `Candidate → Reviewed → Approved`를 **2전이로 구현**(REVIEWED 상태 미도입, reviewed_by/at로 기록) — 상태 하나를 줄여 UI·쿼리 단순화. 발표에서는 대응관계로 설명 (docs/01 §3)
- 08/13 [건태] MVP LLM은 **Claude 단일** 채택 (기획서는 "OpenAI 및 Claude" 표기) — 2주 안에 두 벤더 structured output 차이를 관리할 여유 없음. 교체 지점은 `llm.py` 한 곳이라는 사실로 기획서 취지 유지 (docs/01 §8)
- 08/13 [건태] Safety·Risk Agent를 P1→**P0(결정론 최소 구현)**으로 이동 — 기획서 시나리오 ④가 4개 에이전트를 명시하므로 캐시 라벨 대조만이라도 필수. LLM 해석 계층은 P1 유지
- 08/13 [건태] NOT_BOARD_READY 판정을 4개 사유 코드로 결정론화 (docs/01 §3) — "근거 부족"을 사람이 판단하면 재현되지 않음
- 08/13 [건태] 외부 근거 해석 한계 문구를 소스별로 고정 문장화 (docs/02 §10) — LLM이 caveat을 생성하면 문구가 흔들려 규제 리스크. 커넥터가 자동 부착
- 08/13 [건태] 정량 KPI(Data Latency 70%↓ 등)는 **파일럿 검증 대상**으로 명시, 해커톤 완료 기준은 "대표 가설 1건 완주 + 반복 테스트 통과" (docs/00 §1) — 달성했다고 표현하지 않는다
- 08/13 [건태] 발표 덱·목업의 정본은 **레포 루트 `Demo_Mockup.html`**, `demo/` 사본은 8/12 스냅샷 (docs/05 머리말)
- 08/14 [건태] 심사위원 접속 "상시" 기준 확정 → 클라우드 배포를 P2에서 P0로 승격 — Railway 단일 플랫폼(3서비스+볼륨, 싱가포르 리전), 8/24 첫 배포, 9/3 심사 URL 공유. 라이브 발표 정본은 여전히 로컬+캐시 (docs/07 신설)
- 08/14 [건태] 배포 후에도 main 단일 브랜치 유지 — 안정성은 마일스톤 태그(w1-exit, demo-final 등) + Railway 배포 롤백으로 확보. 9/3 심사 URL 공유 후에는 리허설 통과본만 push
- 08/14 [건태] 맥스튜디오는 개발서버·심사 호스팅으로 쓰지 않음(발표장 백업 머신만) — SQLite 파일 DB 구조라 공유 dev 서버가 불필요하고, 가정용 네트워크는 심사 기간의 단일 장애점
- 08/14 [건태] 코퍼스 포맷: 정본은 .txt(문자 오프셋의 기준) + 같은 원문의 docx/pdf "포장" 렌더 — 실물 양식은 가림 캡쳐에서 구조만 명세로 추출(사내 문서 캡쳐 반입은 이것으로 종료). 업로드 인제스트 연출은 PARKING_LOT
- 08/14 [건태] 실물 원석이 영어·"여러 HCP 묶음" 하이라이트형임을 확인 → `HIGHLIGHT_DOC` 유형 신설 + 추출이 HCP 블록 단위로 interaction 분리(docs/02 §1 필드 3종 추가) — 원석=EN, Field 수집·음성=KO, 데모에서 두 언어 모두 시연. 다국어 원칙: 스키마 언어중립 · verbatim 원언어 보존 · 정규화 계층이 언어 흡수 (docs/02 §3)
- 08/14 [건태] Contract 부트스트랩 채택: 시드 스키마 v0.1도 "코퍼스 샘플 → AI 초안 → 사람 대조·확정"으로 만든다(90분 상한) — "0→1도 AI 제안, 1→N도 AI 제안(SCP), 확정은 항상 사람" 서사를 사실로
- 08/14 [건태] Sense 시각화 채택: 추출 리플레이 연출(저장 결과의 결정론적 재생, 라벨 "추출 완료·결과 재생") + 파이프라인 상태 보드(SQL 카운트) — 둘 다 P1 폴리시
- 08/14 [건태] 코퍼스 본문(prose)은 **Claude Code 세션에서 직접 작성 → `scripts/corpus_bodies/*.json` 커밋**을 기본 경로로. API 키 없이 진행 가능하고 랜덤성이 없어 재현성이 API 경로보다 높다. API 경로는 대안으로 유지(교체 지점 1곳) — 단 백엔드 실행에는 `ANTHROPIC_API_KEY`가 여전히 필수(추출·Screen·Board)
- 08/14 [건태] 영어 원석에서 HCP를 성별 대명사(he/she)로 지칭하지 않는다 — 가명 HCP의 성별을 이름으로 추정하지 않고, 각본 문장과 HCP 명부 간 불일치 버그(실제로 H02·M24에서 발생)를 검증기로 차단. 역할 명사·they·무주어를 쓴다
- 08/14 [건태] PDF 원석 = 합성 학회 참관 보고서 3건 (`CONGRESS_REPORT` 신설, 다중 HCP 블록 분리 대상) — 논문·학회 포스터 "원본"은 반입하지 않음(저작권 + 연구 결과물은 Screen의 외부 공개 근거 영역이라 Sense에 섞으면 사내데이터/공개근거 분리가 흐려짐). 포스터 리얼리티는 보고서 속 "포스터 세션 대화" 장면으로 연출
- 08/15 [건태] **스캐폴딩 선행** — 건태가 8/15–20에 backend 뼈대(스키마·시드·stub API) + 두 앱 셸 + 수직 슬라이스 1줄기를 구축하고, 8/21 킥오프는 "각자 실제 기능 착수"로 시작. 역할: 건태=아키텍트·통합자·Console 오너, **stub은 건태·로직은 폴더 오너** — "각자 빈 화면" 제거 + 통합 지옥 예방 (docs/06 §2.5)
- 08/15 [건태] 협업 모델 공유는 GitHub docs가 정본 — 별도 문서 배포 없이 docs/00·06·CLAUDE.md 갱신 + 메신저에는 링크 한 줄 (단일 진실 원칙 유지)
- 08/15 [건태] Next.js는 스캐폴딩 시점 최신 안정판 **16.3** 채택 (docs 표기 15→16 갱신) — 버전 정합 문서가 node_modules/next/dist/docs에 번들되어 팀원 Claude 세션이 정확한 API를 참조. 각 앱의 AGENTS.md 관리 블록은 유지, 앱별 규칙은 apps/*/CLAUDE.md에 추가
