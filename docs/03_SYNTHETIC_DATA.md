# 03. 합성 데이터셋 설계·생성 가이드

> 목표: "실제 같은 비정형"이면서 **데모가 반드시 성공하도록 신호가 심어진** 코퍼스.
> 전부 가상 인물·기관·발언. 실제 HCP/환자/사내 데이터 절대 사용 금지 (기획서 5-3 §1).
> 담당: 인혁(생성) + 전원(리얼리티 검수). 위치: `backend/data/corpus/`, 생성기: `scripts/generate_corpus.py`.
> **08/14 개정**: 실물 원석 확인에 따라 영어 원석 + `HIGHLIGHT_DOC`(여러 HCP 묶음) 신설 + docx/pdf 포장 렌더 + 다국어 정책 반영 (DECISIONS 08/14).

## 1. 코퍼스 구성 — v3 (문서 320건 / 인사이트 단위 1,118건, HCP 300인, 2021-01 ~ 2026-08)

> **v2(90건) → v3 확장 (08/21 회의)**: 5년 반의 축적분처럼 보여야 "쓰지 못하던 원석"이라는 전제가 산다.
> 문서 1건당 의사 수를 늘리고(하이라이트·학회 6~14 블록), 학회 보고서를 3→22건으로 늘렸다.
> 실측값은 `scripts/generate_corpus.py --dry-run`이 매번 출력한다 — 이 표와 어긋나면 각본이 깨진 것이다.

| 구분 | 문서 | 단위 | 언어 | 형식 |
|---|---|---|---|---|
| **하이라이트 묶음 (HIGHLIGHT_DOC)** | 60 | 688 | EN | 실물 원석 양식 (§3.5). **문서당 HCP 블록 8~14** — 추출이 블록 단위로 interaction 분리 (docs/02 §1) |
| **학회 참관 보고서 (CONGRESS_REPORT)** | 22 | 192 | EN | **PDF 원석** (§3.5·§3.6). 문서당 현장 대화 6~12건 |
| 면담 기록 (MEETING_NOTE) | 120 | 120 | EN | 자유서술, 1문서=1면담 |
| 전화 메모 (CALL_NOTE) | 55 | 55 | EN | 짧은 개조식 |
| 이메일 요약 (EMAIL_SUMMARY) | 45 | 45 | EN | 이메일체 + 번호 매긴 후속 항목 |
| 음성 전사 (VOICE_TRANSCRIPT) | 18 | 18 | KO | 구어체 대화록 — Field 데모용 (§6) |
| **합계** | **320** | **1,118** | EN 1,100 / KO 18 | docx 99 · pdf 22 · txt 320(정본) |

- **신호 분포의 "건"은 인사이트 단위(interaction) 기준**이다 — 하이라이트 1개 문서가 여러 단위를 담는다.
- 기간: **2021-01 ~ 2026-08 (68개월)**. 연도별 단위 157 / 172 / 173 / 205 / 227 / 184(8월까지) — 후반이 두꺼워 추이 곡선이 산다. 미래 날짜는 넣지 않는다(상한 2026-08-21).
- 등장인물: 가상 MSL **12인**(담당 구간 있음 — 5년 반이면 교대가 자연스럽다) × HCP **300인**(가명 `HCP-001~300`, 실제 등장 289인) × 4권역 × 3세팅.
- **실데이터와의 충실도 (08/21 확인 · 정정)**: 실데이터는 **연간 수백 명**의 의료진과 접점이 있고, 그중 **KOL 명부가 약 110명**(2025년 9월까지 집계분)이다 — 110명은 전체가 아니라 부분집합이다(docs/08 §1.2). 코퍼스 v3의 HCP **300인**은 접점 규모와 같은 자릿수라 타당하다. 다만 1인당 3.7단위로 **반복률은 현실보다 낮다**(실데이터는 KOL 쪽이 훨씬 높다). 방향이 유리하므로 그대로 둔다 — 현실에서는 신호가 임계를 더 빨리 넘으므로 **우리 임계값(반복 ≥5 ∧ 독립 HCP ≥3)은 보수적**이다. 파일럿에서 임계값 재조정이 필요할 수 있다는 점만 기록한다.
- **인물 생성 방식**: `HCP-001~024`는 v1 명부를 그대로 보존(손으로 쓴 기존 본문과의 정합), `025~300`은 이름·기관·도시 풀에서 결정론적으로 조합. **기관명은 전부 가상**, 도시는 실제 지명(지리 참조용).

### 1.4 본문 생성 방식 — 조합형 산문 엔진 (08/21 신설)

v2까지 블록 본문은 사람이 직접 쓴 `scripts/corpus_bodies/*.json`(165블록)이었다. v3는 1,118블록이라 손으로 쓸 수 없고 Claude API도 쓸 수 없는 환경이 있다(키·망). 그래서 **손으로 쓴 문장 조각을 시드 고정 PRNG로 조합**한다(`scripts/corpus_v3.py`).

- 기존 165블록은 **그대로 재사용**한다 — 손으로 쓴 산문이 더 좋고, 재현성의 기록이기도 하다.
- 신호 문장은 여전히 스크립트 소유: `frame × detail` 조합(예: S1은 프레임 14 × 상세 14)에서 **전역 유일**한 문장을 뽑는다. 같은 신호라도 320건 어디에도 같은 문장이 두 번 나오지 않는다(검증기가 강제).
- 노이즈 문장은 주제 풀 9종(행정·환자군·접근성·문헌·학회·적정·마감 등) × 슬롯 치환에서 뽑고, **작성자 문체**(narrative / terse / email)와 시기(초기엔 "제품에 익숙해지는 중" 주제)를 반영한다. 오프너는 60%만 붙이고 신호 문장 위치도 무작위 — 모든 블록이 같은 형태로 시작하면 생성물처럼 보인다.
- LLM을 쓰지 않으므로 **같은 커밋 = 같은 코퍼스**다. API 경로(§5-2b)는 남겨두되 v3 기본 경로가 아니다.

### 1.5 언어 정책 (08/14 확정)

- **원석(하이라이트·면담·콜·이메일) = 영어.** 실데이터(약 300건 docx — "쓰지 못하던 원석")가 전부 영어이기 때문 — XCOPRI는 SK 직접 판매 시장이 미국이다(2020년 2분기 출시). 해커톤에는 반입하지 않고 파일럿 검증 카드로만 쓴다.
- **Field 신규 수집·음성 전사 = 한국어** (ko-KR Web Speech).
- **데모에서 두 언어를 모두 시연한다**: 시나리오 ②에서 영문 원석이 구조화되고 → ③에서 한국어 면담이 수집되어 → **같은 canonical 코드로 같은 집계에 합산**되는 순간(예: 영문 S1 + 한국어 음성 S1 → `PEDIATRIC_TRANSITION` +1)이 다국어 전략의 증명이다. 원칙(스키마 언어중립·verbatim 원언어·정규화 계층이 언어 흡수)은 docs/02 §3.
- 화면 라벨과 `summary_ko`는 항상 한국어 (UI는 한국어, 원문은 원언어 그대로).

## 2. 심어야 하는 신호 (Planted Signals) — 데모의 각본

**가설 4개의 역할 분담 (08/21 — 발표 서사)**: ① S1(청소년)은 회사가 **실제로 임상·연령 확대를 진행 중**인 주제(공개 CT.gov 등록 기준)라 "정답을 아는 문제" — 시스템이 현장 대화만으로 같은 신호를 재발견하는 **검증 축**. ② S7(PGTC)·S8(LGS)은 **발굴 축** — 관객이 결말을 모르는 상태로 임계 도달 → 가설 자동 생성 → Board까지 라이브로 본다(S7이 완주 대표). ③ S6(노인)은 In-label **대비 축**. "우리 회사가 수년에 걸쳐 내린 전략 방향을, 이 시스템은 면담 데이터만으로 재구성한다"가 발표의 한 문장이다.

| ID | 내용 | 분포 (실측) | 데모에서의 역할 |
|---|---|---|---|
| **S1 검증 가설 (Development)** | 청소년(12–17세) 약물난치성 초점발작 — 성인 허가라 쓸 수 없어 18세까지 대기하는 치료 공백 | **52건 / 독립 HCP 34인 / 4권역** | HYP-001. **회사가 실제 진행 중인 확대와 일치 = 성능 검증 서사.** 허가 범위 밖이라 상업 액션 차단 시연 겸용 |
| **S6 In-label 대비 가설** | 노인(65+) 병용약물 상호작용·적정 부담 → 자료·교육으로 해소 | **41건 / 27인 / 4권역** | HYP-002(In-label). Development와 나란히 놓아 두 종류의 분리를 증명 |
| **S7 발굴 가설 ①** | 전신 강직-간대발작(PGTC) 치료 선택지 공백 — "초점발작엔 잘 듣는데 전신발작 환자엔 쓸 수 없다"류 | **34건 / 23인 / 4권역** | **HYP-003 — 완주 대표.** 임계 도달 → DRAFT 자동 생성 → Screen → Board → Action Item → Field까지 라이브 |
| **S8 발굴 가설 ②** | 레녹스-가스토 증후군(LGS) — 드롭발작·다발작형에서 기존 약물 한계 | **21건 / 15인 / 4권역** | HYP-004 — 두 번째 발굴. 외부 근거가 얇으면 `NOT_BOARD_READY(NO_EXTERNAL_EVIDENCE)`로 남는 것 자체가 시연 포인트 (§7) |
| **S3 스키마 공백** | "뇌졸중 후 뇌전증(post-stroke epilepsy)" — **v0.1 enum에 없음** | **19건 / 14인** | unmapped 반복 → SCP 자동 제안 → v0.2 승격 (구조 루프). `patient_segment`가 UNSPECIFIED라 **가설 생성 대상에서 제외**된다(docs/01 §3) |
| **S4 자료 요청** | 청소년 용량·안전성 + PGTC 문헌 + 노인 DDI 자료 요청 | **47건** | INFO_REQUEST 집계, 미해결 질문 → Field 체크리스트 (실행 루프 연료) |
| **S5 긍정 신호** | DRE(약물난치성) 성인 환자 발작 감소 경험 | **38건 / 24인** | 대시보드 균형추. 가설 대상 signal_type이 아니라 임계와 무관 |
| **S2 안전성 분기** | 어지러움·졸림·발진·피로·불안정·복시·구역 등 (용어 8종) | **27건** | 자동 분기 시연. 일반 집계에 안 섞이는 것 증명. PV 화면의 용어 집계가 참아진다 |
| **X Critic 차단 미끼** | 허가 범위 밖 단정 2건 + 과일반화 2건 | **4건** | Critic이 막는 장면 (docs/03 §3 규칙 5) |
| 노이즈 | 행정·근황·경쟁 일반론·보험 일반 문의·문헌 잡담 등 | **860단위 (77%)** | 리얼리티. 추출기가 무시 처리하는 것 증명 |

- 총 신호 283건 / 1,118단위. **동거 배치**: S1 단위 9건에 S4(청소년 자료요청), 5건에 S2(AE)를 얹고, S7 단위 6건에 S4(PGTC 문헌)를 얹는다 — 한 발언에서 두 신호가 나오는 현실을 재현한다.
- **임계값 검산 (docs/01 §3: 반복 ≥5 ∧ 독립 HCP ≥3, UNMET_NEED·TREATMENT_BARRIER 한정, UNSPECIFIED 제외)**: 통과 조합이 정확히 4개 — `PEDIATRIC_TRANSITION×UNMET_NEED` `ELDERLY_65_PLUS×TREATMENT_BARRIER` `GENERALIZED_PGTC×UNMET_NEED` `LGS×UNMET_NEED`. 생성기의 `assert_scenario`가 이 집합을 문자 그대로 검증하므로, 각본을 잘못 고치면 생성이 실패한다.

## 3. 리얼리티 규칙 (심사위원이 믿게 만드는 디테일)

1. 문체 혼합: 원석(영어) ① 서술형 보고체 ② ➤ 화살표 개조식(하이라이트형 기본) ③ 이메일체 / 음성 전사(한국어) ④ 구어 전사체. 같은 작성자는 같은 버릇 유지.
2. 오타·비문 5% 의도적 포함. 영어 원석: 오탈자·약어 불일치(cenobamate/XCOPRI/CNB 혼용, 기관명 축약). 한국어 전사: 구어 잉여어·영한 혼용("titration 스케줄이…", XCOPRI/엑스코프리/세노바메이트 혼용).
3. 길이 편차: 개별 문서 300~1,800자, 하이라이트 블록 80~400자. 신호가 없는 문단·블록이 절반 이상.
4. 개인정보 마스킹 데모용 3건: **영어 문서 2건**(가상 실명·미국식 전화 패턴 "Dr. ___, (555) 123-…") + **한국어 음성 1건**("김OO 선생님, 010-XXXX-…") → 수집·적재 단계 마스킹이 두 언어 모두에서 작동함을 연출. `masked_spans` 정답지를 manifest에 미리 계산.
5. 의학적 사실관계는 공개 라벨 수준만 사용, 효능 과장 표현 금지 (Critic 차단 시연용 문장 2건은 예외로 의도 삽입: **"safe for adolescents too"**(허가 범위 벗어난 단정), "works for every patient"(과일반화) → Critic이 잡는 장면).
6. **Development 신호(S1·S7·S8)는 절대 "처방하자"로 쓰지 않는다** — 어디까지나 "쓸 수 없어서 생기는 공백"의 기록이다. 오프라벨 사용을 권유·시사하는 문장은 어느 언어로도 코퍼스에 넣지 않는다. (S7 예: "전신발작 환자에게도 써보고 있다" 금지 → "전신발작 환자에겐 선택지가 없어 답답하다" 형태만.)

### 3.5 하이라이트형 양식 명세 (실물 구조 — 08/14 확보)

> 실물의 가림 캡쳐에서 **구조만** 추출한 명세다. 캡쳐 자체는 폐기했고 레포에 넣지 않는다. 내용·인물·기관은 전부 가상으로 채운다.

```
┌ 1행: 문서 제목 "[조직명] Medical Affairs Highlights"        우상단: 날짜 (파란색 강조)
├ 2행: "From:" + 공동 작성자(가상 MSL 이름들) 나열
├ 회색 배경 밴드(표 셀): 섹션 타이틀 "Field Medical Insights" (가운데 정렬, 볼드)
└ HCP 블록 반복 (4~6개):
   ├ 볼드 헤딩: [First Last], MD, [기관 풀네임], [City], [ST] ([역할코드 RSM|AR])
   │            └ 레지던트면 "PGY2/PGY3 resident" 등 직급 병기
   └ ➤ 화살표 불릿 1~4개: 영어 자유서술, 직접 인용("He stated, '…'") 혼재
       주제 풀: 용량 적정 습관 · 병용약 조정 · 보험/접근성 · 자료 문의(임신 데이터 등)
               · 적응증 확대 일정 질문 · 효능 관찰 소감 · (노이즈) 학회·근황
```

- docx 렌더 템플릿: `scripts/templates/highlight_doc.docx` — 제목행·회색 밴드·볼드 헤딩·➤ 불릿 스타일을 재현하고 python-docx로 채운다.
- **학회 보고서(PDF) 양식**: 표제(가상 학회명·기간·작성 MSL) → 세션별 서술 단락 → **"Poster Session" 단락**(포스터 앞에서 나눈 대화를 서술 — **포스터·논문 원본은 반입하지 않는다**: 저작권 + 그 내용물은 Screen의 외부 공개 근거 영역) → Follow-up 목록. 렌더는 reportlab 단순 서식. 3건 중 1건 이상에 S1(청소년 공백 질문)·S4(자료 요청) 신호를 포스터 대화 장면으로 배치.
- 검수 통과 기준 "우리 회사 문서 같다"에 **양식 재현도 포함**.

### 3.6 학회 보고서 — 실제 학회명 사용 규칙 (08/21 결정)

**왜 학회 보고서가 우리 스토리에 맞는가**: ① MSL이 실제로 쓰는 표준 산출물이다. ② **한 문서에 가장 많은 HCP가 담긴다**(6~12명) — 블록 분리 기능이 가장 잘 드러나는 원석. ③ 평소 방문으로는 못 만나는 권역·기관이 한자리에 모여 지역 분포가 자연스러워진다. ④ 무엇보다 **사내 신호와 외부 근거가 만나는 이음새**다 — 포스터 앞에서 "청소년 데이터는 언제 나오나"를 듣는 장면이 Sense와 Screen 사이의 접점이다.

실제 학회명을 쓰되(팀 결정) 아래 경계를 코드로 지킨다 (`scripts/corpus_v3.py`):

| 지킨다 | 하지 않는다 |
|---|---|
| 학회 **명칭 · 약어 · 연도 · 개최 월** | **개최 도시**, 정확한 개최 일자 |
| **연례 개최가 확실한 미국 학회 3곳만** — AES(12월)·AAN(4~5월)·ANA(9~10월) | 격년 학회(IEC·ECE 등) — **연도 짝을 틀리면 그 자체로 허위 기록**이 되므로 아예 쓰지 않는다 |
| 우리 MSL이 현장에서 나눈 **가상의 대화** | 세션 내용·초록·포스터·발표 데이터의 재현이나 요약 |
| 문서 첫머리에 **학회 참조 고지** 별도 삽입 | 실제 인물·실제 기관의 등장 |

- 고지 문구(`CONGRESS_NOTICE`)가 합성 데이터 고지 **바로 다음 줄**에 들어간다: 학회는 실재하지만 이 기록은 가상이고, 세션 내용은 재현하지 않는다는 명시.
- 22건 배분: AES 2021~2025(10건) · AAN 2021~2026(8건) · ANA 2022~2025(4건). 같은 학회에 보고서가 2~3건인 해가 있는 것은 **여러 MSL이 같은 학회에 참석**하기 때문이며 실제 운영과 같다.
- 본문 첫 줄은 항상 현장성 문구(포스터홀·세션 사이 복도·커피 줄)로 시작해 "세션 요약이 아니라 현장 대화"임을 문서 자체가 말한다.

## 4. 파일 포맷

```
backend/data/corpus/
├── DOC-20251201-001.txt        # 정본 원문 (마스킹 후) — 모든 evidence 오프셋의 기준
├── DOC-20251201-001.docx       # 같은 원문의 "포장" 렌더 (하이라이트형 12건 전부 + 면담 8건)
├── DOC-20260315-007.pdf        # 학회 참관 보고서 3건 (CONGRESS_REPORT)
├── manifest.jsonl              # 한 줄 = 한 인사이트 단위(interaction): docs/02 §1 필드 전부
│                               #   + {file, source_format, language, block_index, doc_char_start/end}
└── ground_truth.jsonl          # 한 줄 = 한 정답 claim: {file, interaction_ref, signal_id, verbatim_quote,
                                #   char_start, char_end(문서 전문 기준), signal_type, patient_segment, barrier_type}
```

- **정본은 항상 `.txt`** — evidence pointer(문자 오프셋)의 기준은 문서 전문 텍스트다 (절대 규칙 #2). docx/pdf는 같은 원문의 포장이며, **렌더 후 텍스트를 재추출해 정본과 일치하는지 자가검증**한다 (우리가 만든 파일이라 파싱이 결정론적 — 실세계 지저분한 PDF 리스크 없음).
- `ground_truth.jsonl`은 **채점용**: 추출 파이프라인의 재현율/수용률 측정(9/2 재현성 테스트)과 리뷰 데모 준비에 쓴다. 서비스 로직은 이 파일을 절대 읽지 않는다.
- 수용률(reviewer acceptance) 정의는 기존과 동일: `승인 claim ÷ 검토 완료(승인+반려) claim` (docs/02 §5.5). 기획서 §6-2의 90% 목표는 파일럿 검증 대상(docs/00 §1), 해커톤에서는 참고 수치만.

## 5. 생성 방법 (`scripts/generate_corpus.py`)

0. **v3 구조 (08/21)**: 각본·인물·산문 엔진은 `scripts/corpus_v3.py`, 렌더·오프셋·검증 파이프라인은 `scripts/generate_corpus.py`(v1에서 검증된 부분을 그대로 유지). 실행: `bash scripts/generate.sh` (uv가 python-docx·reportlab·pypdf를 임시 환경에 설치). 검증만: `python3 scripts/generate_corpus.py --dry-run` (의존성 불필요).
1. **각본 먼저**: 스크립트 상단에 §2 표를 파이썬 dict로 고정 — 문서×블록 단위로 어떤 신호·어떤 표현 변형(언어 포함)이 들어가는지 결정론적으로 배정. 하이라이트형은 블록별 각본을 갖는다.
   - **신호 문장(verbatim)은 스크립트가 소유한다.** LLM은 주변 텍스트만 쓰고 신호 문장은 그대로 심는다 → 데모 각본(S1의 14건/9인/3권역)이 LLM 출력 품질에 흔들리지 않는다.
   - **대명사 규칙**: 영어 원석에서 HCP를 `he/she`로 지칭하지 않는다(역할 명사·they·무주어). 가명 HCP의 성별을 이름으로 추정하지 않고, 각본 문장과 HCP 명부 간 성별 불일치 버그를 구조적으로 없애기 위함. 검증기가 강제한다.
2. **블록 본문(prose) 확보 — 두 경로. 어느 쪽이든 산출물은 동일하다.**
   - (a) **세션 내 작성 (기본)**: Claude Code 세션에서 직접 써서 `scripts/corpus_bodies/*.json`에 저장 (`{"H01": {"1": "본문", …}}`). API 키가 필요 없고, 랜덤성이 없어 재현성이 완전하다. 이 파일들은 **커밋 대상** — 코퍼스 재생성의 입력이자 재현성의 근거다.
   - (b) **Claude API 생성 (대안)**: `.env`에 `ANTHROPIC_API_KEY`가 있으면 본문이 없는 블록만 API로 채운다. temperature 0.8.
   - 스크립트는 (a)를 우선 읽고, 준비본이 규칙을 위반하면 **조용히 API로 넘어가지 않고 즉시 실패**한다(어느 파일 어느 블록인지 표시).
3. **자가 검증**: 신호 문장 정확히 1회 포함 + 블록 구조(불릿·문단 형태) + 성별 대명사 금지 + 길이 범위 → verbatim 위치(문서 전문 기준 char offset) 계산해 ground_truth 기록. 하이라이트형·학회형은 블록 경계(doc_char_start/end)도 함께 기록.
4. **포장 렌더**: 하이라이트형 12건 전부 → `.docx`(§3.5 템플릿), 면담 기록 8건 → `.docx`, 학회 보고서 3건 → `.pdf`(§3.5 양식). 렌더 → 텍스트 재추출 → 정본 일치 자가검증 통과해야 산출.
5. `manifest.jsonl` 생성 → `scripts/seed_db.py`가 documents(문서 전문) + interactions(단위) 분리 적재.
6. **사람 검수(8/15–18)**: 3인이 문서 ~20건씩(하이라이트형은 블록 단위로 훑기) 읽고 어색한 것 flag → 재생성. 통과 기준: "우리 회사 문서 같다" (내용 + 양식).
7. **Contract 부트스트랩 연계 (08/14)**: 검수 통과 샘플 10~20건을 Claude에 주고 "이 데이터에서 뽑아야 할 필드·허용값"을 초안으로 제안받아 v0.1 초안과 대조·반영 (8/18–20, 90분 상한 — docs/00 §6 준용). 채택·기각을 DECISIONS에 기록해 "시드 스키마도 AI 초안 + 사람 확정"을 사실로 만든다.

## 6. 음성 전사 18건 (Field 데모용 — 한국어, v3에서 3→18건)

- **고정 대본 3번(V01~V03)은 손으로 쓴 것을 그대로 유지한다** — 특히 V01은 `apps/field/lib/capture-demo.ts`의 재생 대본과 같은 내용이라 **바꾸면 Field 앱 데모가 어긋난다.**
- 나머지 15건은 조합 엔진이 생성(인사 → 노이즈 → 신호 → 마무리). 한국어 신호 문장은 S1·S7·S6·S5 변형을 쓴다 — **다국어 수렴 시연을 발굴 가설(S7)에서도** 할 수 있게 하기 위함.
- **고정 대본 1번 상세**: 인사(노이즈) → S1 신호 발언("2제 실패한 17세 환자인데 성인 허가라 손을 쓸 수 없다") → **S2 이상사례 발언(분기 연출 포인트, 성인 환자 건으로)** → S4 자료 요청(청소년 용량·안전성) → 마무리.
- 추가 4건(v2): S7 한국어 변형("전신발작 환자에겐 선택지가 없다") 2건 — **다국어 수렴 시연을 발굴 가설에서도** 가능하게 함 — + S6 변형 1건 + 순수 노이즈 1건(신호 없는 면담도 있다는 리얼리티).
- 데모에서는 ① 라이브 마이크(Web Speech ko-KR) 시도 → ② 실패 시 "전사 재생" 버튼으로 이 대본을 타이핑 효과로 흘린다. 두 경로 모두 이후 파이프라인은 동일.
- **다국어 시연 포인트**: 이 한국어 S1 발언이 영문 원석의 S1과 같은 `PEDIATRIC_TRANSITION` 집계에 +1 되는 순간을 Console에서 보여준다 (§1.5).

## 7. 외부 공개 근거 캐시 (`backend/data/cache/`)

시연 전 미리 받아둘 스냅샷 (인혁, 8/26):
- PubMed: `cenobamate adolescent`, `drug-resistant focal epilepsy adolescent treatment options`, `antiseizure medication pediatric pharmacokinetics`, `cenobamate elderly`, `cenobamate drug interaction`, `cenobamate post-stroke epilepsy`, **`cenobamate generalized tonic-clonic`, `primary generalized tonic-clonic seizure treatment options`, `cenobamate Lennox-Gastaut`, `Lennox-Gastaut syndrome treatment gap`** (08/21 — S7·S8 발굴 가설용)
- ClinicalTrials.gov: cenobamate 관련 등록 연구 목록 — **청소년 PK·연령확대 연구와 PGTC 3상 등록이 여기서 잡히는 것이 HYP-001 검증 서사·HYP-003 외부 근거의 핵심** (등록 사실 ≠ 효과 입증 문구 병기, docs/02 §10)
- **⚠ 발굴 가설의 Screen 결과는 실데이터에 달려 있다**: 캐시는 실제 응답만 담는다(조작 금지). LGS(S8) 외부 근거가 얇게 나오면 그대로 `NOT_BOARD_READY(NO_EXTERNAL_EVIDENCE)` 각본으로 확정 — "근거가 없으면 순위를 매기지 않는다"는 원칙의 시연이 되므로 실패가 아니다. 8/26 스냅샷을 받아본 뒤 HYP-004의 결말을 리허설 대본에 고정한다
- openFDA: XCOPRI 라벨(용법·상호작용·고령자 항목), FAERS는 **탐색 신호로만** 표기
- **Drugs@FDA**: XCOPRI 허가 이력·승인 연령 범위 (기획서 5-1에 명시한 소스 — "성인 18세+ 허가"의 공식 근거가 되므로 HYP-001 시연에 필수)
- 파일명 규칙: `{source}_{query_slug}_{YYYYMMDD}.json` — 스냅샷 일자가 화면(`sourceAsOf`)에 표기된다.
- **각 소스의 해석 한계 문구는 docs/02 §10에 고정되어 있다.** 커넥터가 finding을 만들 때 `caveat_ko`를 자동으로 붙인다 (프롬프트가 생성하는 문장이 아니다).
- **배포본 캐시 시드 (08/14)**: 확보한 스냅샷은 `backend/data/fixtures/`로 **커밋**하고, 시드 시 `cache/`로 복사한다 → 클라우드 배포본도 `DEMO_OFFLINE=1` 폴백이 가능해진다 (docs/07).
